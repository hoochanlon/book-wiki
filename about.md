# About

本页只说明这个知识库怎么跑：自动目录从哪来、同类工具怎么做、本地怎么启停。

## 自动目录

VitePress 默认**不会**按文件夹扫描生成左侧目录。默认能力是：

| 能力 | 来源 | 本仓库 |
| --- | --- | --- |
| 页面内 TOC（On this page） | VitePress 自带，读当前页标题 | 有 |
| 左侧 Sidebar | 需手写，或用插件扫 Markdown | 用 `vitepress-sidebar` 自动扫 |
| 顶部 Nav | 需手写 | 目前只有「首页」 |

本仓库的左侧目录由 `vitepress-sidebar` 在构建时扫描根目录 Markdown 生成：文件夹变成分组，文件标题变成菜单项。新增一篇笔记，只要放进对应分类目录，重启或刷新后就会进侧边栏，不必改 `config.mts`。

当前扫描规则：

- 用文件一级标题或 frontmatter 的 `title` 作为菜单名
- 排除 `index.md`、`README.md`
- 首页「开始阅读」走 `/readme`：源文件仍是 `README.md`，由 `rewrites` 映射到小写路由
- 排除 `.vitepress`、`.github`、`node_modules`
- 不按文件名排序，保持目录里的自然顺序

## 主流文档站怎么做自动目录

「文件夹即目录」是静态文档站的常见能力，但实现层不一样：

| 工具 | 自动 Sidebar | 说明 |
| --- | --- | --- |
| VitePress | 需插件 | 本仓库用 `vitepress-sidebar` |
| Astro Starlight | 默认支持 | 不配 `sidebar` 时，按 `src/content/docs/` 文件系统生成；也可对某目录写 `autogenerate` |
| Fumadocs | 默认支持 | `loader()` 从内容目录生成 page tree；可用 `meta.json` 调顺序，不必手写整棵目录 |
| Docusaurus | 官方支持 | `sidebarPath: 'autogenerated'`，按 `docs/` 目录生成 |
| MkDocs Material | 官方支持 | `nav` 可省略，按文件夹自动建导航 |
| Docsify | 半自动 | 默认识别 `_sidebar.md`，也可脚本生成 |
| Hugo | 主题相关 | 多是分类/归档，不是文档 Sidebar |
| GitBook | 约定文件 | 读 `SUMMARY.md`，通常仍要维护一份目录文件 |
| Honkit | 约定文件 | 读 `SUMMARY.md`，通常仍要维护一份目录文件 |

结论：自动目录是主流文档站的标配。Starlight、Fumadocs、Docusaurus、MkDocs 都是官方能力；VitePress 把它留给配置或插件。本仓库用插件扫描，换来「丢进文件夹就能出现在目录里」。

## 本地部署

环境：Node.js 20+，仓库根目录执行。

```bash
npm install
npm run docs:dev
```

默认地址：`http://localhost:5173/book-wiki/`

本仓库 `base` 是 `/book-wiki/`，本地预览也必须带这个前缀，直接打开 `/` 会 404。

常用命令：

```bash
npm run docs:dev      # 开发预览，改 Markdown 热更新
npm run docs:build    # 产出静态文件到 .vitepress/dist
npm run docs:preview  # 本地预览构建结果
```

线上由 GitHub Actions 在 `main` 推送后构建，并发布到 GitHub Pages。本地一般只跑 `docs:dev`。

## 停掉本地进程

`docs:dev` 会占住 5173。终端里直接 `Ctrl+C` 即可。终端关了但进程还在时：

```bash
# 看谁占用了 5173
lsof -i :5173

# 按端口杀掉 VitePress 开发服务
pkill -f "vitepress dev"

# 仍占端口时，按 PID 强制结束
kill -9 <PID>
```

`pkill -f "vitepress dev"` 比乱杀 `node` 安全：只匹配 VitePress 开发命令，不会误杀其他 Node 进程。

确认已停：

```bash
lsof -i :5173
```

无输出即端口已释放，可以重新 `npm run docs:dev`。
